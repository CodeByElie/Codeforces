#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define F(a,b) for (int _ = a; _ < b; _++)
#define DL(L) for(auto e:L) cout << e << " "; cout << endl;

int main() {
    
    return 0;
}